<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="374" onload="init(evt)" viewBox="0 0 1200 374" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:"Verdana"; font-size:12px; fill:rgb(0,0,0); }
#title { text-anchor:middle; font-size:17px; }
#search { opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[var nametype = 'Function:';
var fontsize = 12;
var fontwidth = 0.59;
var xpad = 10;
var inverted = false;
var searchcolor = 'rgb(230,0,230)';
var fluiddrawing = true;
var truncate_text_right = false;]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
          svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            var el = frames.children;
            for(var i = 0; i < el.length; i++) {
                update_text(el[i]);
            }

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad - 100;
            matchedtxt.attributes.x.value = svgWidth - xpad - 100;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
            var params = get_params()
            params.x = el.attributes._orig_x.value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["_orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("_orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["_orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["_orig_" + attr].value;
    e.removeAttribute("_orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));
    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (/^ *\$/.test(txt) || t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.attributes != undefined) {
        orig_load(e, "x");
        orig_load(e, "width");
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, ratio) {
    if (e.attributes != undefined) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = format_percent((parseFloat(e.attributes.x.value) - x) * ratio);
            if (e.tagName == "text") {
                e.attributes.x.value = format_percent(parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value) + (100 * 3 / frames.attributes.width.value));
            }
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = format_percent(parseFloat(e.attributes.width.value) * ratio);
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, ratio);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseFloat(attr.width.value);
    var xmin = parseFloat(attr.x.value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    var ratio = 100 / width;
    // XXX: Workaround for JavaScript float issues (fix me)
    var fudge = 0.001;
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseFloat(a.x.value);
        var ew = parseFloat(a.width.value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew+fudge) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                update_text(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex + fudge >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, ratio);
                update_text(e);
            }
        }
    }
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
        update_text(el[i]);
    }
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseFloat(rect.attributes.width.value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseFloat(rect.attributes.x.value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    var fudge = 0.0001;    // JavaScript floating point
    for (var k in keys) {
        var x = parseFloat(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw - fudge) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="374" fill="url(#background)"/><text id="title" x="50.0000%" y="24.00">Flame Graph</text><text id="details" x="10" y="357.00"> </text><text id="unzoom" class="hide" x="10" y="24.00">Reset Zoom</text><text id="search" x="1090" y="24.00">Search</text><text id="matched" x="1090" y="357.00"> </text><svg id="frames" x="10" width="1180"><g><title>epirust`dashmap::mapref::entry::Entry&lt;K,V,S&gt;::or_insert (74 samples, 0.12%)</title><rect x="0.0194%" y="117" width="0.1196%" height="15" fill="rgb(227,0,7)"/><text x="0.2694%" y="127.50"></text></g><g><title>epirust`&lt;dashmap::iter::Iter&lt;K,V,S,M&gt; as core::iter::traits::iterator::Iterator&gt;::next (1,328 samples, 2.15%)</title><rect x="24.8242%" y="69" width="2.1470%" height="15" fill="rgb(217,0,24)"/><text x="25.0742%" y="79.50">e..</text></g><g><title>epirust`&lt;core::iter::adapters::chain::Chain&lt;A,B&gt; as core::iter::traits::iterator::Iterator&gt;::fold (4,366 samples, 7.06%)</title><rect x="19.9159%" y="101" width="7.0584%" height="15" fill="rgb(221,193,54)"/><text x="20.1659%" y="111.50">epirust`&lt;..</text></g><g><title>epirust`core::ops::function::impls::_&lt;impl core::ops::function::FnMut&lt;A&gt; for &amp;mut F&gt;::call_mut (4,365 samples, 7.06%)</title><rect x="19.9175%" y="85" width="7.0568%" height="15" fill="rgb(248,212,6)"/><text x="20.1675%" y="95.50">epirust`c..</text></g><g><title>epirust`&lt;dashmap::iter::Iter&lt;K,V,S,M&gt; as core::iter::traits::iterator::Iterator&gt;::next (1,361 samples, 2.20%)</title><rect x="26.9744%" y="101" width="2.2003%" height="15" fill="rgb(208,68,35)"/><text x="27.2244%" y="111.50">e..</text></g><g><title>epirust`dashmap::DashMap&lt;K,V,S&gt;::clear (966 samples, 1.56%)</title><rect x="29.1795%" y="101" width="1.5617%" height="15" fill="rgb(232,128,0)"/><text x="29.4295%" y="111.50"></text></g><g><title>epirust`hashbrown::map::HashMap&lt;K,V,S&gt;::retain (964 samples, 1.56%)</title><rect x="29.1828%" y="85" width="1.5585%" height="15" fill="rgb(207,160,47)"/><text x="29.4328%" y="95.50"></text></g><g><title>epirust`hashbrown::map::HashMap&lt;K,V,S&gt;::insert (4,476 samples, 7.24%)</title><rect x="34.2478%" y="85" width="7.2363%" height="15" fill="rgb(228,23,34)"/><text x="34.4978%" y="95.50">epirust`ha..</text></g><g><title>epirust`hashbrown::raw::RawTable&lt;T&gt;::insert (2,636 samples, 4.26%)</title><rect x="37.2225%" y="69" width="4.2616%" height="15" fill="rgb(218,30,26)"/><text x="37.4725%" y="79.50">epiru..</text></g><g><title>epirust`dashmap::mapref::entry::Entry&lt;K,V,S&gt;::or_insert (6,660 samples, 10.77%)</title><rect x="30.8027%" y="101" width="10.7671%" height="15" fill="rgb(220,122,19)"/><text x="31.0527%" y="111.50">epirust`dashmap:..</text></g><g><title>epirust`&lt;core::iter::adapters::Filter&lt;I,P&gt; as core::iter::traits::iterator::Iterator&gt;::next (78 samples, 0.13%)</title><rect x="42.7225%" y="85" width="0.1261%" height="15" fill="rgb(250,228,42)"/><text x="42.9725%" y="95.50"></text></g><g><title>epirust`&lt;dashmap::DashMap&lt;K,V,S&gt; as dashmap::t::Map&lt;K,V,S&gt;&gt;::_get (238 samples, 0.38%)</title><rect x="43.0717%" y="69" width="0.3848%" height="15" fill="rgb(240,193,28)"/><text x="43.3217%" y="79.50"></text></g><g><title>epirust`&lt;dashmap::DashMap&lt;K,V,S&gt; as dashmap::t::Map&lt;K,V,S&gt;&gt;::_get (540 samples, 0.87%)</title><rect x="43.7782%" y="37" width="0.8730%" height="15" fill="rgb(216,20,37)"/><text x="44.0282%" y="47.50"></text></g><g><title>epirust`&lt;core::iter::adapters::Filter&lt;I,P&gt; as core::iter::traits::iterator::Iterator&gt;::next (730 samples, 1.18%)</title><rect x="43.5632%" y="53" width="1.1802%" height="15" fill="rgb(206,188,39)"/><text x="43.8132%" y="63.50"></text></g><g><title>epirust`epirust::agent::Citizen::move_agent_from (912 samples, 1.47%)</title><rect x="43.4565%" y="69" width="1.4744%" height="15" fill="rgb(217,207,13)"/><text x="43.7065%" y="79.50"></text></g><g><title>epirust`epirust::geography::area::Area::get_random_point (197 samples, 0.32%)</title><rect x="44.9309%" y="69" width="0.3185%" height="15" fill="rgb(231,73,38)"/><text x="45.1809%" y="79.50"></text></g><g><title>epirust`epirust::agent::Citizen::goto_area (1,459 samples, 2.36%)</title><rect x="42.8922%" y="85" width="2.3587%" height="15" fill="rgb(225,20,46)"/><text x="43.1422%" y="95.50">e..</text></g><g><title>epirust`&lt;dashmap::DashMap&lt;K,V,S&gt; as dashmap::t::Map&lt;K,V,S&gt;&gt;::_get (14,212 samples, 22.98%)</title><rect x="53.4104%" y="53" width="22.9763%" height="15" fill="rgb(210,31,41)"/><text x="53.6604%" y="63.50">epirust`&lt;dashmap::DashMap&lt;K,V,S&gt; as ..</text></g><g><title>epirust`&lt;core::iter::adapters::Filter&lt;I,P&gt; as core::iter::traits::iterator::Iterator&gt;::next (18,728 samples, 30.28%)</title><rect x="48.2063%" y="69" width="30.2773%" height="15" fill="rgb(221,200,47)"/><text x="48.4563%" y="79.50">epirust`&lt;core::iter::adapters::Filter&lt;I,P&gt; as cor..</text></g><g><title>epirust`epirust::allocation_map::AgentLocationMap::get_area_map (1,297 samples, 2.10%)</title><rect x="76.3867%" y="53" width="2.0968%" height="15" fill="rgb(226,26,5)"/><text x="76.6367%" y="63.50">e..</text></g><g><title>epirust`&lt;dashmap::DashMap&lt;K,V,S&gt; as dashmap::t::Map&lt;K,V,S&gt;&gt;::_get (1,053 samples, 1.70%)</title><rect x="78.4836%" y="69" width="1.7024%" height="15" fill="rgb(249,33,26)"/><text x="78.7336%" y="79.50"></text></g><g><title>epirust`epirust::allocation_map::AgentLocationMap::get_area_map (70 samples, 0.11%)</title><rect x="80.1875%" y="69" width="0.1132%" height="15" fill="rgb(235,183,28)"/><text x="80.4375%" y="79.50"></text></g><g><title>epirust`epirust::geography::area::Area::get_random_point (116 samples, 0.19%)</title><rect x="80.3007%" y="69" width="0.1875%" height="15" fill="rgb(221,5,38)"/><text x="80.5507%" y="79.50"></text></g><g><title>epirust`c2_chacha::guts::refill_wide (536 samples, 0.87%)</title><rect x="80.8633%" y="53" width="0.8665%" height="15" fill="rgb(247,18,42)"/><text x="81.1133%" y="63.50"></text></g><g><title>epirust`c2_chacha::guts::refill_wide::impl_avx2 (536 samples, 0.87%)</title><rect x="80.8633%" y="37" width="0.8665%" height="15" fill="rgb(241,131,45)"/><text x="81.1133%" y="47.50"></text></g><g><title>epirust`rand::Rng::sample (817 samples, 1.32%)</title><rect x="80.4882%" y="69" width="1.3208%" height="15" fill="rgb(249,31,29)"/><text x="80.7382%" y="79.50"></text></g><g><title>epirust`epirust::agent::Citizen::move_agent_from (22,618 samples, 36.57%)</title><rect x="45.2510%" y="85" width="36.5662%" height="15" fill="rgb(225,111,53)"/><text x="45.5010%" y="95.50">epirust`epirust::agent::Citizen::move_agent_from</text></g><g><title>epirust`&lt;dashmap::DashMap&lt;K,V,S&gt; as dashmap::t::Map&lt;K,V,S&gt;&gt;::_get (2,690 samples, 4.35%)</title><rect x="84.4426%" y="69" width="4.3489%" height="15" fill="rgb(238,160,17)"/><text x="84.6926%" y="79.50">epiru..</text></g><g><title>epirust`epirust::agent::Citizen::update_infection_dynamics (4,347 samples, 7.03%)</title><rect x="81.8172%" y="85" width="7.0277%" height="15" fill="rgb(214,148,48)"/><text x="82.0672%" y="95.50">epirust`e..</text></g><g><title>epirust`epirust::agent::Citizen::perform_operation (29,289 samples, 47.35%)</title><rect x="41.6426%" y="101" width="47.3511%" height="15" fill="rgb(232,36,49)"/><text x="41.8926%" y="111.50">epirust`epirust::agent::Citizen::perform_operation</text></g><g><title>epirust`rand::Rng::sample (80 samples, 0.13%)</title><rect x="88.8643%" y="85" width="0.1293%" height="15" fill="rgb(209,103,24)"/><text x="89.1143%" y="95.50"></text></g><g><title>epirust`dashmap::DashMap&lt;K,V,S&gt;::entry (3,817 samples, 6.17%)</title><rect x="90.2740%" y="85" width="6.1709%" height="15" fill="rgb(229,88,8)"/><text x="90.5240%" y="95.50">epirust`..</text></g><g><title>epirust`dashmap::lock::compare_exchange (883 samples, 1.43%)</title><rect x="95.0174%" y="69" width="1.4275%" height="15" fill="rgb(213,181,19)"/><text x="95.2674%" y="79.50"></text></g><g><title>epirust`epirust::allocation_map::AgentLocationMap::entry (4,622 samples, 7.47%)</title><rect x="89.0276%" y="101" width="7.4723%" height="15" fill="rgb(254,191,54)"/><text x="89.2776%" y="111.50">epirust`ep..</text></g><g><title>epirust`dashmap::lock::compare_exchange (111 samples, 0.18%)</title><rect x="98.0600%" y="69" width="0.1795%" height="15" fill="rgb(241,83,37)"/><text x="98.3100%" y="79.50"></text></g><g><title>epirust`hashbrown::map::HashMap&lt;K,V,S&gt;::insert (771 samples, 1.25%)</title><rect x="98.2394%" y="69" width="1.2465%" height="15" fill="rgb(233,36,39)"/><text x="98.4894%" y="79.50"></text></g><g><title>epirust`hashbrown::raw::RawTable&lt;T&gt;::insert (370 samples, 0.60%)</title><rect x="98.8877%" y="53" width="0.5982%" height="15" fill="rgb(226,3,54)"/><text x="99.1377%" y="63.50"></text></g><g><title>epirust`dashmap::DashMap&lt;K,V,S&gt;::insert (1,530 samples, 2.47%)</title><rect x="97.0205%" y="85" width="2.4735%" height="15" fill="rgb(245,192,40)"/><text x="97.2705%" y="95.50">ep..</text></g><g><title>epirust`epirust::allocation_map::AgentLocationMap::insert (1,859 samples, 3.01%)</title><rect x="96.4999%" y="101" width="3.0054%" height="15" fill="rgb(238,167,29)"/><text x="96.7499%" y="111.50">epi..</text></g><g><title>epirust`rand::rngs::thread::thread_rng (83 samples, 0.13%)</title><rect x="99.5813%" y="101" width="0.1342%" height="15" fill="rgb(232,182,51)"/><text x="99.8313%" y="111.50"></text></g><g><title>libdyld.dylib`tlv_get_addr (76 samples, 0.12%)</title><rect x="99.7155%" y="101" width="0.1229%" height="15" fill="rgb(231,60,39)"/><text x="99.9655%" y="111.50"></text></g><g><title>epirust`epirust::epidemiology_simulation::Epidemiology::simulate (61,596 samples, 99.58%)</title><rect x="0.2619%" y="117" width="99.5813%" height="15" fill="rgb(208,69,12)"/><text x="0.5119%" y="127.50">epirust`epirust::epidemiology_simulation::Epidemiology::simulate</text></g><g><title>epirust`&lt;core::future::from_generator::GenFuture&lt;T&gt; as core::future::future::Future&gt;::poll (61,759 samples, 99.84%)</title><rect x="0.0016%" y="133" width="99.8448%" height="15" fill="rgb(235,93,37)"/><text x="0.2516%" y="143.50">epirust`&lt;core::future::from_generator::GenFuture&lt;T&gt; as core::future::future::Future&gt;::poll</text></g><g><title>epirust`main (61,853 samples, 100.00%)</title><rect x="0.0016%" y="277" width="99.9968%" height="15" fill="rgb(213,116,39)"/><text x="0.2516%" y="287.50">epirust`main</text></g><g><title>epirust`std::rt::lang_start_internal (61,853 samples, 100.00%)</title><rect x="0.0016%" y="261" width="99.9968%" height="15" fill="rgb(222,207,29)"/><text x="0.2516%" y="271.50">epirust`std::rt::lang_start_internal</text></g><g><title>epirust`std::rt::lang_start::_{{closure}} (61,853 samples, 100.00%)</title><rect x="0.0016%" y="245" width="99.9968%" height="15" fill="rgb(206,96,30)"/><text x="0.2516%" y="255.50">epirust`std::rt::lang_start::_{{closure}}</text></g><g><title>epirust`std::sys_common::backtrace::__rust_begin_short_backtrace (61,853 samples, 100.00%)</title><rect x="0.0016%" y="229" width="99.9968%" height="15" fill="rgb(218,138,4)"/><text x="0.2516%" y="239.50">epirust`std::sys_common::backtrace::__rust_begin_short_backtrace</text></g><g><title>epirust`epirust::main (61,853 samples, 100.00%)</title><rect x="0.0016%" y="213" width="99.9968%" height="15" fill="rgb(250,191,14)"/><text x="0.2516%" y="223.50">epirust`epirust::main</text></g><g><title>epirust`tokio::runtime::handle::Handle::enter (61,853 samples, 100.00%)</title><rect x="0.0016%" y="197" width="99.9968%" height="15" fill="rgb(239,60,40)"/><text x="0.2516%" y="207.50">epirust`tokio::runtime::handle::Handle::enter</text></g><g><title>epirust`tokio::runtime::context::enter (61,853 samples, 100.00%)</title><rect x="0.0016%" y="181" width="99.9968%" height="15" fill="rgb(206,27,48)"/><text x="0.2516%" y="191.50">epirust`tokio::runtime::context::enter</text></g><g><title>epirust`tokio::runtime::enter::Enter::block_on (61,853 samples, 100.00%)</title><rect x="0.0016%" y="165" width="99.9968%" height="15" fill="rgb(225,35,8)"/><text x="0.2516%" y="175.50">epirust`tokio::runtime::enter::Enter::block_on</text></g><g><title>epirust`&lt;core::future::from_generator::GenFuture&lt;T&gt; as core::future::future::Future&gt;::poll (61,853 samples, 100.00%)</title><rect x="0.0016%" y="149" width="99.9968%" height="15" fill="rgb(250,213,24)"/><text x="0.2516%" y="159.50">epirust`&lt;core::future::from_generator::GenFuture&lt;T&gt; as core::future::future::Future&gt;::poll</text></g><g><title>epirust`epirust::epidemiology_simulation::Epidemiology::new (87 samples, 0.14%)</title><rect x="99.8577%" y="133" width="0.1407%" height="15" fill="rgb(247,123,22)"/><text x="100.1077%" y="143.50"></text></g><g><title>epirust`epirust::geography::grid::Grid::generate_population (73 samples, 0.12%)</title><rect x="99.8804%" y="117" width="0.1180%" height="15" fill="rgb(231,138,38)"/><text x="100.1304%" y="127.50"></text></g><g><title>all (61,855 samples, 100%)</title><rect x="0.0000%" y="325" width="100.0000%" height="15" fill="rgb(231,145,46)"/><text x="0.2500%" y="335.50"></text></g><g><title>epirust`0x3 (61,855 samples, 100.00%)</title><rect x="0.0000%" y="309" width="100.0000%" height="15" fill="rgb(251,118,11)"/><text x="0.2500%" y="319.50">epirust`0x3</text></g><g><title>libdyld.dylib`start (61,854 samples, 100.00%)</title><rect x="0.0016%" y="293" width="99.9984%" height="15" fill="rgb(217,147,25)"/><text x="0.2516%" y="303.50">libdyld.dylib`start</text></g></svg></svg>